# ALO

Listen together.

ALO is the public product and repository name. Use uppercase **ALO** in copy;
`alo` remains the executable name. Do not introduce a second public name.

## Assets

- [App mark](../../Resources/ALOLogo-1024.png): the existing ALO logo, shared with the packaged Mac app.
- [GitHub share image](alo-social.png): the mark, ALO wordmark, and “Listen together.” on a light background. Used in the repository README and social preview.

Keep the mark’s proportions and give it room. Do not recolor it to match a page,
add a badge, or replace the app icon with the wide share image.

## Compatibility

The GitHub home is https://github.com/theShyamsindhia/ALO. Downloads retain their
`ALO-macos-arm64` filenames. The bundle ID, local data, Keychain, Bonjour names,
and legacy audio interfaces are compatibility contracts, not public branding.
Preserve them so existing installations continue to work.

## Share-image source

Created with the built-in image-generation tool, using the existing app mark as
the reference. The app mark itself was not replaced. Final prompt:

> Use case: compositing. Asset type: GitHub repository social-preview image, landscape exactly 1280x640 pixels, PNG under 1 MB if possible. Input image 1 is the existing ALO app logo and must retain its identity: the same dark rounded animal face, two white eyes, and blue/green/purple iridescent edge. Do not redesign the mascot or add facial features. Compose that mark into a clean, confident, minimal ALO brand card on a solid warm-white background. Large black rounded heavy sans-serif wordmark text exactly 'ALO', with smaller text exactly 'Listen together.' beneath it. The existing mark and wordmark form a balanced horizontal lockup with generous outer whitespace. No extra text, no badges, no browser frame, no drop shadows, no interface mockup. Keep every element clear and away from the crop edges. Preserve the original logo as faithfully as possible; only layout, scale, background and the specified typography are new.

The generated PNG is 1774 × 887 px (2:1), below GitHub’s 1 MB upload limit.
